{
  "name": "garm-code",
  "productName": "Cicada",
  "version": "0.8.1",
  "description": "Cicada \u2014 an agentic Python IDE powered by a local GGUF model (Qwen2.5-Coder 3B).",
  "main": "src/main/main.js",
  "author": "Cicada",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "check": "node scripts/check.js",
    "icon": "electron scripts/make-icon.js",
    "dist:win": "electron scripts/make-icon.js && electron-builder --win && npm run verify:bundle",
    "dist:mac": "electron scripts/make-icon.js && electron-builder --mac && npm run verify:bundle",
    "dist": "electron-builder",
    "verify:bundle": "node scripts/verify_bundle.js"
  },
  "dependencies": {
    "@langchain/core": "^1.2.9",
    "@langchain/langgraph": "^1.4.14",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "katex": "^0.16.11",
    "monaco-editor": "^0.52.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.cicada.ide",
    "productName": "Cicada",
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "asar": true,
    "afterPack": "scripts/after-pack-sign.js",
    "files": [
      "src/**/*",
      "assets/cicada-logo.svg",
      "assets/cicada-onboarding.png",
      "package.json"
    ],
    "mac": {
      "icon": "build/icon.png",
      "category": "public.app-category.developer-tools",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "arm64"
          ]
        }
      ]
    },
    "dmg": {
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "win": {
      "icon": "build/icon.png",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "artifactName": "${productName}-Setup-${version}-${arch}.${ext}",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "Cicada"
    },
    "portable": {
      "artifactName": "${productName}-Portable-${version}-${arch}.${ext}"
    }
  }
}
