# Vortex Solver – 3D Surface Manifold

A physics solver that computes the velocity field of a vortex ring using the Biot-Savart law, evaluates the vorticity field via finite differences, and extracts an isosurface of the vorticity magnitude to visualise the vortex core as a 3D surface manifold.

## Usage

1. Install dependencies:
